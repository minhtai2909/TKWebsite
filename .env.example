# Tên miền thật của website, dùng cho canonical URL, sitemap, robots và Open Graph.
# Bắt buộc đặt đúng trước khi deploy, nếu không SEO sẽ trỏ sai tên miền.
NEXT_PUBLIC_SITE_URL=https://thienkhoi.vn

# Endpoint nhận dữ liệu từ biểu mẫu liên hệ (Formspree, Google Apps Script, n8n, webhook nội bộ...).
# Bỏ trống thì biểu mẫu sẽ báo người dùng gọi hotline thay vì gửi đi.
CONTACT_WEBHOOK_URL=
